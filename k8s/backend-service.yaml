apiVersion: v1
kind: Service
metadata:
  name: cook-with-me-backend
  namespace: cook-with-me
  labels:
    app: cook-with-me-backend
spec:
  type: ClusterIP
  selector:
    app: cook-with-me-backend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
